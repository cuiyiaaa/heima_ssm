<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.ssm.mapper.UsersMapper">

    <resultMap id="users_Map" type="UserInfo">
        <id property="id" column="id"></id>
        <result property="email" column="email"></result>
        <result property="username" column="username"></result>
        <result property="password" column="password"></result>
        <result property="status" column="status"></result>

        <collection property="roleList" column="id" select="com.itheima.ssm.mapper.RoleMapper.findRoleByUserId"></collection>
    </resultMap>

    <!--根据用户名查询-->
    <select id="findUserInfoByName" parameterType="java.lang.String" resultMap="users_Map">
        SELECT * FROM USERS WHERE USERNAME =#{username}
    </select>

    <!--查询所有-->
    <select id="findUserInfoAll" resultType="UserInfo">
        SELECT * FROM USERS
    </select>

    <!--根据ID查询-->
    <select id="findUserInfoById" parameterType="java.lang.String" resultMap="users_Map">
        SELECT * FROM USERS WHERE ID =#{ID}
    </select>

    <!--根据角色ID查询与角色相关的用户-->
    <select id="findUserInfoByRoleId" parameterType="java.lang.String" resultType="UserInfo">
        SELECT * FROM USERS WHERE ID IN (SELECT USERID FROM  users_role WHERE ROLEID=#{id})
    </select>

    <!--保存-->
    <insert id="saveUserInfo" parameterType="UserInfo">
        INSERT INTO USERS(EMAIL,USERNAME,PASSWORD,PHONENUM,STATUS) VALUES(#{email},#{username},#{password},#{phoneNum},#{status})
    </insert>

</mapper>

