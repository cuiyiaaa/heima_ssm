<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.ssm.mapper.PermissionMapper">

    <resultMap id="permission_Map" type="Permission">
        <id property="id" column="id"/>
        <result property="permissionName" column="PERMISSIONNAME"/>
        <result property="url" column="URL"/>
        <collection property="rolesList" column="id" select="com.itheima.ssm.mapper.RoleMapper.findRoleByPermissionID"/>
    </resultMap>

    <!--根据权限ID获取对应的权限信息-->
    <select id="findPermissionById" parameterType="java.lang.String" resultMap="permission_Map">
        SELECT * FROM permission WHERE ID=#{id}
    </select>

    <!-- 根据角色id查询，角色下的所有权限-->
    <select id="findPermissionByRoleId" parameterType="java.lang.String" resultType="permission">
        SELECT * FROM permission WHERE ID IN (SELECT PERMISSIONID FROM role_permission WHERE roleid=#{roleId})
    </select>

    <!--查询所有权限信息-->
    <select id="findPermissionAll" resultType="Permission">
        SELECT * FROM PERMISSION
    </select>

    <insert id="savePermission" parameterType="Permission">
        INSERT INTO PERMISSION(PERMISSIONNAME,URL) VALUES(#{permissionName} ,#{url})
    </insert>

</mapper>